export type AppTheme = {
    colors: {
        primart: string,
        background: string,
        foreground: string,
        error: string,
        typography: string,
        input: string
    }
}